general {
    col.active_border = rgba(869394AA)
    col.inactive_border = rgba(3c494aAA)
}

misc {
    background_color = rgba(0e1415FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(0e1415FF)
        col.text = rgba(dee4e4FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(dee4e4), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(dee4e4), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(dee4e4), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(51d9e2AA) rgba(51d9e277),pinned:1
